# Guidelines

Directives an agent follows — how to behave, collaborate, and write code. Each file is one
self-contained rule.

- `collaboration/` — working with an agent (decisions, check-ins, surfacing, response style)
- `coding-style/` — how code should be written
- `debugging/` — how to approach diagnosis
- `git/` — version-control discipline
